export interface ProfileFormValues {
  full_name: string;
  phone: string;
  bio: string;
}

export interface RoleMeta {
  color: string;
  bg: string;
  icon: React.ReactNode;
  label: string;
}
